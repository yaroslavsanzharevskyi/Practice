namespace Leetcode.Problems;

public class LowestPositiveInt
{
    public int FindLowestPositiveInt(int[] a)
    {
        var lowestPositive = 1;
        // Array.Sort(a);
        // // -5-6-2 0 1 2 4

        // GetPositiveNumbersStartIndex(0, a.Length / 2);
        // GetPositiveNumbersStartIndex((a.Length / 2) + 1, a.Length);
        // if ()
        return lowestPositive;
    }
}